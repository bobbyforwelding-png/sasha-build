Hi Bobby,

I apologize for the confusion earlier. I accidentally deleted the file while renaming it before you downloaded it!

HOW TO GET THE APP RIGHT NOW:
1. Look in the File Explorer on the left, but note that the APK might be hidden because it is over 1MB.
2. Click the "Download as ZIP" button in the top right.
3. Once downloaded, extract the ZIP file.
4. Inside the extracted folder, you will find `sasha_app.apk`.
5. Move `sasha_app.apk` to your tablet and tap on it to install.

IF YOU ARE USING GITHUB:
To answer your question in the screenshot:
- You should select **Private** (so only you can see the repository).
- I have also fixed the bug in the GitHub Action! It was missing a small environment file. Now, when you sync to GitHub, the action will successfully build the app, and you can download the APK from the "Artifacts" section in the GitHub Actions tab.

Again, the easiest way right now is to just click "Download as ZIP". The `sasha_app.apk` file is guaranteed to be inside.
